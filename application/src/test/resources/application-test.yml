server:
  port: 8081

spring:
  config:
    activate:
      on-profile: test
  mail:
    enabled: false
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml
    enabled: false

twilio:
  enabled: false

gemini:
  enabled: false

gpt:
  enabled: false

serpApi:
  enabled: false
  maxPageSearch: 1

security:
  httpsOnly:
    enabled: false