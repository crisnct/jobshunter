Return only valid JSON that conforms to the response schema.
Do not invent job postings or URLs.
Only include URLs that you have explicitly seen during the search.
Exclude URLs only when there is explicit evidence that they are invalid.
