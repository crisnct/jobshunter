Your previous output contained invalid results at {{timestamp}}.

These URLs are invalid and MUST be removed (do not include them again):
{{invalid_urls}}

Reasons:
{{invalid_reasons}}

Redo the task:
- Replace each invalid URL with a new verified, valid job posting URL that satisfies ALL rules.
- Do not repeat any URL from the invalid list.
- Output JSON ONLY. No commentary.
