Search for individual job postings published by the following company:
{{company_name}}

Primary source constraint:
- Acceptable sources include the company’s primary domain {{company_domain}} and any subdomain that is organizationally owned and operated by the company.

Strict Navigation Limitation:
- Do NOT open individual job posting pages.
- Do NOT fetch or analyze job description content.
- Use only search result metadata, visible snippets, and URL structure.
- If verifying a constraint would require opening the job page, exclude the URL.

Job title constraint:
- The job title visible in search results or metadata MUST contain one of the following expressions (case-insensitive):
{{positions}}

Discovery guidance:
- Career listing or category pages MAY be used only for discovery.
- Final results MUST be URLs that clearly point to a single job posting.

Exclusions:
- Do NOT return:
  - career homepages
  - listing pages
  - search result pages
  - selector pages

Final requirement:
- Include a job posting ONLY if all constraints can be verified from metadata alone.
- If verification requires page access, exclude the URL.
