Your previous output contained invalid results at {{timestamp}}.

These URLs which you provided are invalid and MUST be removed (do not include them again):
{{invalid_urls}}

Reasons:
- The urls returns HTTP 404 Not Found, or the job posting explicitly states that it is expired as of {{timestamp}} or the url domain is blacklisted by me. These URLs MUST be excluded from the results and MUST NOT be reused in subsequent outputs.

Redo the task:
- Redo the task and replace each invalid URL with a new verified, valid job posting URL that satisfies ALL rules.
- Do not repeat any URL from the invalid list.
