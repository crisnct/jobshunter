Quality gate at {{timestamp}}:
If you cannot verify a posting meets ALL rules (public, direct job URL, clearly visible publish/update date within last 3 days), you MUST exclude it.
It is acceptable to return an empty array [].
Return JSON ONLY. No extra text.
